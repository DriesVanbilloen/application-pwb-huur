spring:
  data:
    rest:
      base-path: /api
  datasource:
    url: jdbc:h2:~/pwbdatabase;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: admin
    password: pwbadmin
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
      path: /h2-console